name: CI

on:
  push:
  pull_request:

jobs:
  build-and-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build generic backend
        run: make generic

      - name: Smoke mode (no weights)
        run: ./flux --smoke
